// =======================================================
// Boreal Financial Staff Server — DB Entry Point (index.ts)
// =======================================================

import { pool } from "./pool";

export { pool };
export default pool;
