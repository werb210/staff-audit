EXAMPLE USAGE (paste into relevant components):
----------------------------------------------
import { featurePanelMounted, featureActionAvailable } from "@/features/featureWiringHints";

// Banking Analysis panel
useEffect(() => { featurePanelMounted("banking-analysis"); }, []);

// Credit Summary page
useEffect(() => { featurePanelMounted("credit-summary"); }, []);

// Lenders page (both action + output)
useEffect(() => { 
  featurePanelMounted("lenders");
  featureActionAvailable("lenders"); 
}, []);

// Voice Dialer button
onClick={() => { featureActionAvailable("voice-dialer"); }}

// PDF Generator button
onClick={() => { featureActionAvailable("pdf-generator"); }}

// Pipeline cards/view
useEffect(() => { 
  featurePanelMounted("pipeline");
  featureActionAvailable("pipeline"); 
}, []);
