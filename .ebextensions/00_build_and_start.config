container_commands:
  01_build_server:
    command: "npm run build:server"
  02_build_client:
    command: "npm run build:client:fast"
  03_set_permissions:
    command: "chmod -R 755 dist || true"
  04_confirm:
    command: "echo '✅ Build completed for EB environment'"

option_settings:
  aws:elasticbeanstalk:application:environment:
    NODE_ENV: "production"
    PORT: "8080"
